#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        if command -v systemctl >/dev/null 2>&1; then
            # Stop service before removal/upgrade
            systemctl stop created.service || true
        fi
    ;;
esac

exit 0

